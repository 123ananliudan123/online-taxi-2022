server:
  # 默认是8080
  port: 8081

spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  application:
    name: api-passenger

  # 下面的redis不配置也可以
  redis:
    host: 127.0.0.1
    port: 6379
    database: 0